version: '3.8'

services:
  QSD:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: QSD-node
    ports:
      - "8080:8080"
    volumes:
      - ./QSD.db:/app/QSD.db
      - ./QSD.log:/app/QSD.log
      - ./config:/app/config
    environment:
      - USE_SCYLLA=false
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:8080"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

